class Article {
  String title, details, location, time, image;
  List<String> members, category;
  Article(this.title, this.details, this.image, this.location, this.time,
      this.members, this.category);
}
